{
  "name": "zoom-dummy-users",
  "version": "1.0.0",
  "description": "Spawn dummy participants in a Zoom meeting using Playwright",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "install-browser": "npx playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.50.0"
  }
}
